// stack all ur entities file here
import { User } from './User';
import { SessionEntity } from './Session';
const entities = [User, SessionEntity];

export { User, SessionEntity };
export default entities;
